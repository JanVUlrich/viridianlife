---
import { formatOptionsDate } from '../lib/dateformat'

interface Props {
	date: string | Date;
}

const { date } = Astro.props;
---

<time datetime={new Date(date).toISOString()}>
	{
		new Date(date).toLocaleDateString('en-us', formatOptionsDate)
	}
</time>
